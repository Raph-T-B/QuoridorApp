<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QuoridorMaui.Pages._1VSBotPage"
             Title="_1VSBotPage">
    <Grid ColumnDefinitions="10*,80*,10*"
          RowDefinitions="10*,80*,10*" >
        <Grid Grid.Row="1" Grid.Column="1"
              RowDefinitions="10*,50*,20*">
            <Label Text="1 VS BOT" Grid.Row="0" HorizontalTextAlignment="Center"/>
            <HorizontalStackLayout Grid.Row="1" VerticalOptions="Center">
                <Label Text="Nom du joueur"/>
                <!--
                    <Button Content="Button!" Click="OnButtonClick" Command="{Binding WhateverCommand}" />      -->
            </HorizontalStackLayout>
            <VerticalStackLayout>
            <Label Text="1 VS BOT" Grid.Row="2" HorizontalTextAlignment="Center"/>
            <Label Text="1 VS BOT" Grid.Row="2" HorizontalTextAlignment="Center"/>
            <Label Text="1 VS BOT" Grid.Row="2" HorizontalTextAlignment="Center"/>
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>