<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:QuoridorMaui.Views"
             xmlns:mylayouts="clr-namespace:QuoridorMaui.MyLayouts"
             x:Class="QuoridorMaui.Pages.Page1VS1"
             Title="1 VS 1">
    <ContentPage.Resources>
        <ResourceDictionary>
            <x:Double x:Key="VerticalSpacing">2</x:Double>
            <x:Double x:Key="HorizontalSpacing">2</x:Double>
            <DataTemplate x:Key="cellTemplate">
                <Border Stroke="Black" StrokeThickness="1" BackgroundColor="White">
                    <Label Text="{Binding}"
                           TextColor="Black"
                           FontSize="20"
                           FontAttributes="Bold"
                           HorizontalOptions="Fill"
                           VerticalOptions="Fill"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center"/>
                </Border>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30,20" VerticalOptions="Center" HorizontalOptions="Center">
            <!-- Titre -->
            <Label Text="1 VS 1" FontSize="40" FontAttributes="Bold" HorizontalOptions="Center"/>

            <!-- Choix du BO -->
            <HorizontalStackLayout Spacing="15" HorizontalOptions="Center">
                <views:Bouton Texte="Bo1" WidthRequest="80" HeightRequest="40" FontSize="18" Clicked="Bo1_Tapped"/>
                <views:Bouton Texte="Bo3" WidthRequest="80" HeightRequest="40" FontSize="18" Clicked="Bo3_Tapped"/>
                <views:Bouton Texte="Bo5" WidthRequest="80" HeightRequest="40" FontSize="18" Clicked="Bo5_Tapped"/>
            </HorizontalStackLayout>

            <!-- Joueur 1 -->
            <Frame BorderColor="Black" CornerRadius="20" Padding="15" BackgroundColor="White">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Joueur 1" FontSize="22" FontAttributes="Bold" TextColor="Black"/>
                    <Entry x:Name="EntryJoueur1" Placeholder="Nom du joueur 1" HorizontalTextAlignment="Center" TextColor="Black"/>
                    <Label Text="Couleur :" FontSize="16" TextColor="Black"/>
                    <HorizontalStackLayout x:Name="ColorButtonsJ1" Spacing="10" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Joueur 2 -->
            <Frame BorderColor="Black" CornerRadius="20" Padding="15" BackgroundColor="White">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Joueur 2" FontSize="22" FontAttributes="Bold" TextColor="Black"/>
                    <Entry x:Name="EntryJoueur2" Placeholder="Nom du joueur 2" HorizontalTextAlignment="Center" TextColor="Black"/>
                    <Label Text="Couleur :" FontSize="16" TextColor="Black"/>
                    <HorizontalStackLayout x:Name="ColorButtonsJ2" Spacing="10" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Nombre de murs -->
            <Label Text="Même nombre pour les deux" FontSize="16" HorizontalOptions="Center" TextColor="White"/>
            <Entry x:Name="EntryNbMurs" Placeholder="Nombre de murs" Keyboard="Numeric" HorizontalTextAlignment="Center" WidthRequest="200" TextColor="White"/>

            <!-- Plateau de jeu -->
            <Frame BorderColor="Black" Padding="10" BackgroundColor="Black">
                <mylayouts:MatrixLayout 
                    BindableLayout.ItemsSource="{Binding GameBoard.FlatMatrix}"
                    BindableLayout.ItemTemplate="{StaticResource cellTemplate}"
                    NbColumns="{Binding GameBoard.NbColumns}"
                    NbRows="{Binding GameBoard.NbRows}"
                    HorizontalSpacing="{StaticResource HorizontalSpacing}"
                    VerticalSpacing="{StaticResource VerticalSpacing}"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
            </Frame>

            <!-- Boutons principaux -->
            <VerticalStackLayout Spacing="15" Margin="0,30,0,0" HorizontalOptions="Center">
                <views:Bouton Texte="Lancer la partie" WidthRequest="350" HeightRequest="80" FontSize="35" Clicked="LancerPartie_Tapped"/>
                <views:Bouton Texte="Retour" WidthRequest="200" HeightRequest="50" Clicked="Retour_Tapped"/>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>